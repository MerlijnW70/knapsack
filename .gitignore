# Rust build output
/target
*.pdb
*.exe
*.rlib
*.rmeta

# editor / OS cruft
*.rs.bk
*.swp
*.log
.DS_Store
Thumbs.db

# Knapsack runtime state + config backups (should never be committed)
.knapsack/
.store/
metrics.jsonl
*.knapsack-bak-*

# Claude Code project-local state (permissions, local commands) — user-specific.
.claude/

# ANVIL per-project gate config — user-specific shortcut for source-tree gates
# (see H:/anvil/README.md). Not part of the project contract.
.anvil/

# Note: Cargo.lock IS committed (this crate builds a binary).
